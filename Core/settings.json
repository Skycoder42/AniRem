{
	"allowSearch": true,
	"allowRestore": true,

	"entries": {
		"openEntries": {
			"title": "&Open Animes in browser",
			"tooltip": "When you long press animes, for example to remove the new seasons mark, the browser will automatically open the animes site",
			"type": "bool",
			"default": false,
			"searchKeys": ["open", "click", "browser", "new"],
			"properties": null
		}
	}
}
